1→import { useQuery } from '@tanstack/react-query'; 2→import { api } from '../services/api'; 3→ 4→export function useMcpStatus() { 5→ return useQuery({ 6→ queryKey: ['mcp', 'status'], 7→ queryFn: () => api.mcp.status(), 8→ staleTime: 60000, 9→ refetchInterval: 60000, 10→ }); 11→}