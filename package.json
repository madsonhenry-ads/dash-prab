{
  "name": "trafficboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev:mcp-mock": "cd mcp-mock && npm run dev",
    "dev": "npx concurrently -n server,client,mock -c blue,green,yellow \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:mcp-mock\"",
    "build:server": "cd server && npm run build",
    "build:client": "cd client && npm run build",
    "build": "npm run build:server && npm run build:client",
    "start": "cd server && npm run start",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install && cd ../mcp-mock && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}